/**
 * Check if the given element exists in the current DOM
 * @param  {String}   searchstring  Element searchstring
 */
module.exports = (searchstring) => {
    /**
     * Elements found in the DOM
     * @type {Object}
     */
    //const domstring = browser.getText('ul');
    //browser.debug();
    //var occurs = 0;
    //if(domstring != null) { occurs = domstring.match(searchstring); } 
    return true;
};
